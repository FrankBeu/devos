#################################################################################################### XDG-ALIASES
###
##
#
alias cobra='cobra --config ${XDG_CONFIG_HOME}/cobra/config.toml'

alias ccze='ccze -F ${XDG_CONFIG_HOME}/ccze/config'
# alias freecad='freecad -u ${XDG_CONFIG_HOME}/FreeCAD/user.cfg -s ${XDG_CONFIG_HOME}/FreeCAD/system.cfg'

# alias mongo="${XDG_CONFIG_HOME}/mongodb mongo"

# alias sqlite3='sqlite3 -init ${XDG_CONFIG_HOME}/sqlite3/sqliterc'

# alias wget='wget --hsts-file=${XDG_CACHE_HOME}/wget-hsts"'
################################################################################### END XDG-ALIASES
